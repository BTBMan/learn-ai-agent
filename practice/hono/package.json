{
  "name": "hono",
  "type": "module",
  "scripts": {
    "wr": "wrangler",
    "login": "pnpm wr login",
    "dev": "pnpm wr dev",
    "deploy": "pnpm wr deploy --minify",
    "cf-typegen": "pnpm wr types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
